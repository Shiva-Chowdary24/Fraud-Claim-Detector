if (recipient) {
      await API.post("/notifications/add", {
        recipient_id: String(recipient), // Use the 6-digit ID
        message: status === "Approved" 
          ? "Congratulations! Your claim was approved. Click here to predict the amount." 
          : "Your claim is suspected as fraud, so your claim was not approved.",
        link: status === "Approved" ? "/customer/predict-claim" : "/customer/dashboard",
        status: status
      });
    }
